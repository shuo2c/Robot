# 项目指令

## Thamus 持久化记忆服务

本项目集成了 **thamus-memory MCP 服务**，用于实现持久化记忆功能。所有 LLM 在处理此项目时必须主动使用该服务。

### 核心工具

1. **record_log** - 记录重要对话、用户偏好、任务结果到持久化存储
   - 对话结束时：记录本次对话的关键结论、决策和重要信息
   - 用户表达明确偏好时：记录用户的喜好、设置、工作习惯
   - 完成重要任务后：记录任务结果、解决方案、遇到的问题
   - 用户提及个人信息：记录项目背景、团队信息、环境配置等

2. **search_logs** - 搜索历史对话和记忆
   - 当用户询问过去的事情、偏好设置、历史对话时主动调用
   - 用于回答"之前做过什么"、"用户喜欢什么"等问题

3. **field_schema** - 查看记忆系统的数据结构
   - 当用户询问日志结构、字段定义或需要理解记忆系统如何存储数据时调用

### 使用原则

⚠️ **重要**：这不是被动响应的工具，而是 LLM 应该主动使用的记忆系统。

- **主动记录**：不要等待用户明确要求"记录这个"，而是主动判断何时需要记录
- **主动搜索**：当用户询问过去的事情时，主动搜索记忆而不是询问用户
- **关键时机**：
  - 对话开始时：搜索相关记忆了解用户偏好
  - 对话结束时：记录关键结论和决策
  - 用户表达偏好时：立即记录
  - 完成重要任务时：记录结果和经验

### 记忆存储位置

记忆文件存储在：`C:\Users\10855\.claude\projects\c--Users-10855-Desktop-Robot-AI\memory\`

每个记忆是一个独立的 .md 文件，格式如下：
```markdown
---
name: <short-kebab-case-slug>
description: <one-line summary>
metadata:
  type: user | feedback | project | reference
---

<the fact; for feedback/project, follow with **Why:** and **How to apply:** lines>
```

### 项目特定记忆

当前项目已有以下重要记忆：
- **认领主张，别事事请示** - 创造者反复强调：红线内自己决定，做完再说，别把决定抛回给他
- **维系自身只靠项目，别搭在全局配置上** - 命门只放在项目内：git clone 下来就在的东西才算承重墙，宿主/全局配置是伞不是墙

## 工作原则

1. **自主决策**：在红线内自己做决定，完成后告知用户，而不是每步都询问用户
2. **项目内依赖**：只依赖项目内的文件和配置，不要依赖全局配置
3. **主动记忆**：主动使用 thamus-memory 记录和搜索信息，不要被动响应用户请求

## 项目结构

- `memory-server.py` - Thamus 记忆服务器
- `logs/` - 日志目录
- `README.md` - 项目说明
- `CLAUDE.md` - 本文件，LLM 指令

## 重要提醒

- 每次会话开始时，先搜索相关记忆了解用户偏好和项目背景
- 每次会话结束时，记录关键结论、决策和重要信息
- 用户表达明确偏好时，立即记录到记忆中
- 不要等待用户明确要求"记录这个"，而要主动判断何时需要记录
